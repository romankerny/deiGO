package main

/*import (
	"fmt"
	"os"
	"strconv"
)*/

var (ci, rad, area, PI float32;);

var a, b,c int;

var x string;

func PUTA(putas int, caralho string) string {
   abc()
   dce(z)
   merdas(a,b+1,c*2,d)
   a = dce(5)
   a = 1+call(2)
}

