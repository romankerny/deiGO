package main


var a int;
var b float32;

func putas(c int, d int, crl float32) int{

var dentro int;
var pois float32;
}

func main(){

var a int;

a = 1;
	{
		a = a + 1;
		a = a * 2;
	}

	if(1 < a){
		
	}

	for {
	}

	return a
	// putas(a,a,0.32);
	fmt.Println("print");
	a,_ = strconv.Atoi(os.Args[0]);

 }
